server:
  port: 8081
spring:
  application:
    name: spring-mvc-demo
  profiles: prod
  datasource:
    url: jdbc:postgresql://localhost:5432/proddb
    username: prod_user
    password: prod_pass
logging:
  level:
    root: DEBUG
